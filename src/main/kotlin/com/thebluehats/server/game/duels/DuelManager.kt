package com.thebluehats.server.game.duels

class DuelManager {
    private val duels = hashSetOf<Duel>()

    fun startDuel(duel: Duel) {}

    private fun provisionMap() {}

    private fun findNext() {
        val size = duels.size

        // duels.indices.forEach { i ->
        //     if (get)
        // }
    }
}
